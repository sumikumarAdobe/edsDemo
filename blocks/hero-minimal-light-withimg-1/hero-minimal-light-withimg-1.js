/**
 * Hero (CSS-only block). Decoration handled by default section/block markup.
 * @param {Element} block The block element
 */
// eslint-disable-next-line no-unused-vars
export default function decorate(block) {}
